import React, {Component, useState} from "react";

function Motion() {
    return(
    <div style = {{ width: '100%', 
    height: '100vh', 
    margin: 0, 
    padding: 0, 
    backgroundImage: `linear-gradient(40deg, var(--color-bg1), var(--color-bg2))`,
    zIndex: -1, 
    position: 'fixed',}}>

    </div>
    )

}

export default Motion

